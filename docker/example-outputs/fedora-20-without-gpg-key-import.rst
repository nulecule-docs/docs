.. _docker-example-outputs-fedora-20-without-gpg-key-import:

Example Output without Importing GPG Key
========================================

The following output shows the warning and dialog issued during the
build of an image, should the GPG key with which RPM packages are
signed not be imported.

.. literalinclude:: fedora-20-without-gpg-key-import.txt
    :linenos:
    :emphasize-lines: 44,49-54
